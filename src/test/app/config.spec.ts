// import { JetKitCdkApp } from "../.."
// import { IConfig } from "../../app/config"

// // custom configuration
// interface ICustomConfig extends IConfig {
//   foo: string
// }

describe("Config", () => {
  it("can be read", () => {
    //     const app = new JetKitCdkApp<ICustomConfig>({
    //       config: {
    //         bundle: { banner: "awesome compiler banner" },
    //         foo: "bar",
    //       },
  })

  //     expect(app.config).toMatchObject({
  //       bundle: { banner: "awesome compiler banner" },
  //       foo: "bar",
  //     })
  //   })
})
